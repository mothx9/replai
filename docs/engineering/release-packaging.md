# RELEASE.PACKAGING.0 evidence

Status: source and qualification machinery under construction. Executed results
are recorded here only after the packaging source commit is immutable and the
native target runs complete.

## Contract

This dossier owns G3/G4 evidence for the unpublished `replai 0.1.0` candidate:
Cargo packaging, Rust 1.98.1, unpacked and fresh consumers, docs.rs-style docs,
the deterministic `replai-c-sdk-0.1.0` source bundle, relocatable pkg-config and
CMake installations, C ABI 1, and the external debugger-style consumer.

The artifact source revision may precede the final documentation/evidence
carrier. Every retained artifact identifies the exact source revision and tree
from which it was built. A later carrier does not silently rename those bytes.

## Executed evidence

Pending immutable packaging-source commit and native qualification.

## Residual publication gates

No crate was published and no tag or GitHub Release was created. Registry
publisher authentication and hosted docs.rs behavior remain publication-time
checks. E3/V0 remains a later boundary.
